{
  "name": "external-image-importer",
  "version": "4.0.0",
  "private": true,
  "description": "Local development environment for the External Image Importer WordPress plugin.",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "env:start": "wp-env start",
    "env:stop": "wp-env stop",
    "env:destroy": "wp-env destroy",
    "env:cli": "wp-env run cli wp",
    "env:logs": "wp-env logs",
    "test:php": "wp-env run tests-cli --env-cwd=wp-content/plugins/external-image-importer vendor/bin/phpunit -c phpunit.xml.dist"
  },
  "devDependencies": {
    "@wordpress/env": "^10.0.0"
  }
}
